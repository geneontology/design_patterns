# specify python as the language
language: python

# python versions to be used for testing
python:
   - "3.5"
 
install:
   - pip install -r requirements.txt
   

# test script
script:
   - wget https://github.com/dosumis/dead_simple_owl_design_patterns/blob/master/spec/DOSDP_schema_core.yaml
   - python src/test_patterns.py
